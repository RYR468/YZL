# 益志领 · 学生↔导师匹配 MVP 依赖
# 注：当前开发环境无法联网 pip install，引擎核心仅用 Python 标准库即可运行（配置用 JSON）。
# 以下为 v1.5 完整功能（飞书读写 / LLM 理由 / YAML 配置 / 测试）所需：

flask>=3.0           # Web 框架（后端路由/模板/会话）—— app.py 必需
lark-oapi>=1.3.0     # 飞书开放平台官方 SDK（读写多维表格）—— v1.5 FeishuLoader 需要
openai>=1.0          # LLM 生成匹配理由（兼容任意 OpenAI 协议接口，如豆包/DeepSeek/智谱）
pyyaml>=6.0          # 配置若用 .yaml 格式需要；不装则用 .json
pytest>=7.0          # 跑 tests/（不装也可 `python tests/test_matching.py`）
gunicorn>=21.0       # 生产 WSGI 服务器（Zeabur/容器部署用）
flask-limiter>=3.5   # 频率限制(防刷报名/登录暴力破解)
